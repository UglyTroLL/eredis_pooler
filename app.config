[
  {eredis_pooler, [
      {pools, [
        [
          {name, redis1},
          {group, redis},
          {max_count, 10},
          {init_count, 3},
          {start_mfa, {eredis, start_link, ["localhost", 6379]}}
        ]
      ]}
    ]
  }
].